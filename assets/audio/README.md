# Audio Files

Place your audio file here:

- `power_line.mp3` - The audio line "Every power must be earned." that plays once when the player commits to the journey.

The file should be a standard MP3 audio file. Once added, uncomment the audio playback code in `app/(tabs)/index.tsx` (around line 70-85).

